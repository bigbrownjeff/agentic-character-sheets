node_modules/
.DS_Store

# Generated card images — PNGs are build artifacts; SVGs are tracked
render/cards/*.png

# OS cruft
Thumbs.db
*.swp
*.swo

# scratch preview screenshots
.preview/

# downstream / Instagram card export — kept local, out of the repo
render/
local/

# generated beat-card images (local viewing only)
site/cards/
